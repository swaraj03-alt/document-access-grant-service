version: '3.9'

services:
  db:
    image: postgres:15
    container_name: grants_db
    restart: always

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: grants_db

    ports:
      - "5432:5432"